# detectors package

# Place individual detector implementations here, e.g.:
# - signature_port_scan.py
# - signature_syn_flood.py
# - behavioral_rate_detector.py

# Each detector should implement a `handle(packet)` method that returns
# either None (no alert) or a dict describing the alert.
